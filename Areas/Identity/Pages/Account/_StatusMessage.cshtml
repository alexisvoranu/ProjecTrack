@model string

@if (!string.IsNullOrEmpty(Model))
{
    var statusClass = Model.StartsWith("Error")
        ? "alert-danger"
        : "alert-success";

    <div class="alert @statusClass" role="alert" style="margin-top: 1rem;">
        @Html.Raw(Model)
    </div>
}
